import { BrowserRouter, Routes } from "react-router-dom";

function Router() {
  return (
    <BrowserRouter>
      <Routes>
        {/* Routes go here */}
      </Routes>
    </BrowserRouter>
  );
}

export default Router;